# FastAPI watcher
SECRET_KEY=change-me
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
DATABASE_URL=sqlite:///./data/data.db

# Email SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_TLS=true
FROM_EMAIL=

# Timezone for scheduler
TIMEZONE=Asia/Bangkok
RENDER_JS=true
RENDER_TIMEOUT=20
RENDER_POST_WAIT_SECONDS=3

# Debug diagnostics (optional)
# When true, saves fetched HTML and screenshots under ./data/artifacts
DEBUG_DUMP_ARTIFACTS=false
# Where to store artifacts (relative to project root or absolute)
DEBUG_ARTIFACTS_DIR=./data/artifacts
# Optional CSS selector to wait for during JS rendering (leave blank to skip)
DEBUG_WAIT_SELECTOR=
