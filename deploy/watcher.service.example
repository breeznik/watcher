[Unit]
Description=Watcher FastAPI Service
After=network.target

[Service]
User=www-data
WorkingDirectory=/opt/watcher
EnvironmentFile=/opt/watcher/.env
ExecStart=/usr/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target